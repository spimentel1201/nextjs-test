import { Box } from '@mui/material'
import { styled } from '@mui/material/styles'

export const WrapperGraphicsBar = styled(Box)(() => ({
    position: 'relative',
    margin: 'auto',
    height: '80vh',
    width: '80vw',
}))
